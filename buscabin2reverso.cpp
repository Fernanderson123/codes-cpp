#include <iostream>
using namespace std;
int main (){
	int k,n=10;// elemento a ser encontrado e tamanho do vetor
	int v[n];// vetor na posição n que tem tamanho=10
	int i, posicao=-1;//i auxilia no caminho das posicões do vetor e posição sem posicionamento = -1
	for(i=0;i<n;i++) cin >> v[i];//pega o i e de 0 a n você insere valores (insere 10 valores)
		cin >> k;//posicao
		i=0;
		while (i<n){
		if (n==k){
		//posicao=n;
		} i++;
}		cout << i << endl;
	return 0;
}
